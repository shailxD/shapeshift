import { SvgToReactTool } from "@/app/_components/tools/svg-to-react-tool";

export const metadata = {
  title: "SVG to React Component",
  description:
    "Convert SVG files to React components with TypeScript support and syntax highlighting.",
};

export default function SvgToReactPage() {
  return <SvgToReactTool />;
}
